import styled from "styled-components";

const ChipWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
`;

const Chip = styled.div`
  border-radius: 10px;
  padding: 5px;
  background-color: #f9fafb;
`;

export { ChipWrapper, Chip };
